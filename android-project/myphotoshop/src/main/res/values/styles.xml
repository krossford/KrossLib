<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="android:actionBarStyle">@style/MyActionBarStyle</item>
        <item name="actionBarStyle">@style/MyActionBarStyle</item>
    </style>

    <style name="MyActionBarStyle" parent="Widget.AppCompat.ActionBar">
        <item name="android:background">@color/ui</item>
        <item name="background">@color/ui</item><!--兼容这一行必须要啊-->

        <item name="android:titleTextStyle">@style/MyActionBarTitleTextStyle</item>
        <item name="titleTextStyle">@style/MyActionBarTitleTextStyle</item>



    </style>

    <style name="MyActionBarTitleTextStyle">
        <item name="android:textColor">@color/ui_text</item>
    </style>

</resources>